# Golf Course Feature Mapper — Python dependencies
# Pin strategy: minimum known-good versions listed here.
# After first successful install, run:  pip freeze > versions.lock
# and commit versions.lock to record the exact resolved environment.

# ── Core ML ────────────────────────────────────────────────────────────────
# ultralytics: YOLO26-seg requires a build from Jan 2026 or later.
# Verify the exact model identifier (e.g. "yolo26n-seg.pt") in the ultralytics
# docs before calling model.train(). Fallback to yolo11-seg if unavailable.
torch>=2.2.0
torchvision>=0.17.0
ultralytics>=8.3.0

# ── Geospatial / SAM ───────────────────────────────────────────────────────
# segment-geospatial (samgeo): verify tms_to_geotiff signature and SamGeo
# model_version="sam2" kwarg name against installed version.
segment-geospatial>=0.40.0
rasterio>=1.3.9
geopandas>=1.0.0
shapely>=2.0.4
pyproj>=3.6.1
geocube>=0.5.0

# ── OSM data access ────────────────────────────────────────────────────────
overpy>=0.7.0
osm2geojson>=0.2.1

# ── Visualization / confirmation gate ─────────────────────────────────────
leafmap>=0.36.0
folium>=0.17.0

# ── Config & utilities ─────────────────────────────────────────────────────
pydantic>=2.5.0
PyYAML>=6.0.1
tqdm>=4.66.4
requests>=2.31.0

# ── Data / numerics ────────────────────────────────────────────────────────
numpy>=1.26.4
opencv-python>=4.9.0.80
scipy>=1.13.0
pandas>=2.2.2

# ── Hugging Face (SAM 2.1 weights; SAM 3 if use_sam3=true) ────────────────
transformers>=4.44.0
huggingface-hub>=0.24.0

# ── Testing ────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-cov>=5.0.0
